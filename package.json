{
  "name": "codecollectors",
  "version": "1.0.0",
  "description": "Store your favorite or most useful code snippets for personal use or to share them with others through interactions with other users!",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --host",
    "dev": "NODE_ENV=development nodemon index.js",
    "test": "NODE_ENV=test jest --verbose --runInBand",
    "deploy:test": "NODE_ENV=production jest --runInBand",
    "cypress:open": "cypress open",
    "test:e2e": "cypress run",
    "build:ui": "rm -rf dist && cd ./client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "config": "^3.3.11",
    "cors": "^2.8.5",
    "cypress": "^13.6.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "jest": "^29.7.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.36.0",
    "sequelize-hierarchy-next": "^1.0.0"
  },
  "devDependencies": {
    "supertest": "^6.3.4"
  }
}
